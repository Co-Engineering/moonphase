{
  "name": "@moonphase/desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Moonphase desktop shell",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json && electron .",
    "start": "electron .",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "electron": "^33.0.2",
    "typescript": "^5.6.3"
  }
}
