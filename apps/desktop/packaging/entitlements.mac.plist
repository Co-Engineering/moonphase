<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!--
	The standard set Electron itself documents for a hardened-runtime build
	(electronjs.org/docs/latest/tutorial/code-signing): V8 needs JIT and
	unsigned executable memory, and Electron's own libraries and any native
	Node modules need library validation relaxed to load. Without these, a
	hardened-runtime build can crash on launch rather than merely lose a
	permission it never used.
	-->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.allow-dyld-environment-variables</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- The renderer talks to a user-configured Moonphase server over HTTP(S)
	     and WebSocket, and the preview relay dials out too. -->
	<key>com.apple.security.network.client</key>
	<true/>
</dict>
</plist>
